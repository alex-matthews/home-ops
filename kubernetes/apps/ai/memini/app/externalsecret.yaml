---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: memini
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: memini-api-keys
    template:
      data:
        api-keys.yaml: |
          keys:
            - name: alex
              secret: "{{ .ADMIN_KEY }}"
              home: alex
              admin: true
            - name: claude
              secret: "{{ .CLAUDE_KEY }}"
              home: alex
            - name: codex
              secret: "{{ .CODEX_KEY }}"
              home: alex
  dataFrom:
    - extract:
        key: memini
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: memini-db
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: memini-secret
    template:
      data:
        MEMINI_POSTGRES_DSN: "postgresql://memini:{{ .DB_PASSWORD }}@postgres-rw.database.svc.cluster.local:5432/memini"
  dataFrom:
    - extract:
        key: memini
