---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: external-external-dns-rules
spec:
  groups:
    - name: external-external-dns.rules
      rules:
        - alert: ExternalExternalDNSStale
          expr: |
            time() - external_dns_controller_last_sync_timestamp_seconds{job="external-external-dns"} > 60
          for: 5m
          annotations:
            summary: >-
              ExternalDNS controller ({{ $labels.job }}) has not synced in over 5 minutes.
          labels:
            severity: critical
