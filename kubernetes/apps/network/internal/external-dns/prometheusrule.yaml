---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: internal-external-dns-rules
spec:
  groups:
    - name: internal-external-dns.rules
      rules:
        - alert: InternalExternalDNSStale
          expr: |
            time() - external_dns_controller_last_sync_timestamp_seconds{job="internal-external-dns"} > 60
          for: 5m
          annotations:
            summary: >-
              ExternalDNS controller ({{ $labels.job }}) has not synced in over 5 minutes.
          labels:
            severity: critical
